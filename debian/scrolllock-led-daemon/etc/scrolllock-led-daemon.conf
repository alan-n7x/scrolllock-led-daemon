[daemon]
# Keyboard device path (overrides auto-detection)
; device = /dev/input/event4

# LED brightness file path or name suffix (scrolllock, capslock, numlock)
; led = scrolllock

# Key to listen for (default: KEY_SCROLLLOCK)
; key = KEY_SCROLLLOCK

# Enable debug logs (true/false)
; verbose = false
